## server port
#server:
#  port: 8761

# Spring
spring:
  application:
    # 应用名称
    name: eureka-demo
  profiles:
    active: dev
#eureka:
#  client:
#    sevice-url:
#      defaultZone: http://localhost:${server.port}/eureka
#    register-with-eureka: false
#    fetch-registry: false